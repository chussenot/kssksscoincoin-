- unless @campaigns.nil?
  .ui.items
    - @campaigns.each do |c|
      .item.campaign
        .content
          .name= c.name
          %p.description= c.description
          .ui.striped.progress
            .bar{style:'width: 30%'}
          %p Raise B40 of B100

        .ui.button.add_pledge.red{"data-bitcoin_address"=> c.bitcoin_address}= 'Fund this project'
