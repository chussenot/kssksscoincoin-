- unless @campaigns.nil?
  .ui.items
    - @campaigns.each do |c|
      .item.campaign
        .content
          .name= c.name
          %p.description= c.description
        .ui.button.add_pledge.red{"data-bitcoin_address"=> c.bitcoin_address}= 'Add pledge'
