%h1 KssKssCoinCoin
- unless @campaigns.nil?
  .ui.items
    - @campaigns.each do |c|
      .item.campaign
        .content
          .name= c.name
          %p.description= c.description
        .ui.button.green= 'Add an option !'
